export default function LoginPage() {
  return (
    <div className="page">
      <h1>Login</h1>
      <p style={{ color: "var(--color-fg_muted)", marginTop: "var(--space-2)" }}>
        Coming soon — Anmeldung wird in Ticket #2 implementiert.
      </p>
    </div>
  );
}
